pub use ammber_core::{
    constants::{
        contracts::*, get_token_map, CHAIN_ID, DEVNET_IO_PUBKEY, NODE, SYMBOL_TO_ADDR, TOKEN_MAP,
    },
    utils::*,
    Error,
};

// pub type Result<T, E = Error> = core::result::Result<T, E>;
